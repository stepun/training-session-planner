services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: training-generator-frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - node_modules:/app/node_modules
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
      - VITE_S3_URL=https://web-storage.sh3.su/api/v1/service-account-credentials
      - VITE_S3_ACCESS_KEY=pCXbOilHuSP1d4NlGztc
      - VITE_S3_SECRET_KEY=O5xVdSiF3JCXJCIQQcgDe6iDJ9ZpuRXdkWfZXzWk
      - VITE_S3_API=s3v4
      - VITE_S3_PATH=auto
    stdin_open: true
    tty: true

volumes:
  node_modules: